# Voice and accessibility review — 2026-09-08

Reviewed the current ToolsEnabled app source (starting at `4826597c`), its focused tests, and the retained desktop/mobile verification records. The subsequent requested defect repair is based on `82703d4c`. This document records source changes and measured checks; the designated final promotion session owns the separate LIVE receipt.

The Accessibility entry now appears once in **Settings → Appearance & reading → Accessibility**, at `#/settings?category=accessibility`. Settings search also finds it through “accessibility”, “voice”, and “hands-free”. It remains available in Simple, Advanced, and Expert modes. The panel is part of the page layout and leaves with Settings. Pending action confirmations still appear on the screen being controlled and disappear when resolved; they do not bring back the global entry point. Returning to Settings shows the host's current agent and control scope. Enable/stop are immediate session actions, independent of Save settings.

The voice contact/dock retains its existing lifetime across page navigation. Accessibility remains off on application startup, and the host still requires its existing confirmation for enabling and for each action.

| Item | Current evidence and impact | Disposition |
| --- | --- | --- |
| **Linux accepted an unusable desktop-control scope — fixed** | The original probe reproduced `enabled: true` followed by “This desktop adapter requires Windows.” The host now exposes supported scopes and refuses desktop requests on Linux/macOS with `ACCESSIBILITY_SCOPE_UNSUPPORTED` before consent or audit. Settings disables the unsupported choice and explains why. | Closed by the requested repair. Generic “enable accessibility” now chooses application control outside Windows; explicit desktop voice requests are consumed locally and refused. Windows desktop behavior and its Unrestricted-permission requirement remain intact. Four new host regressions failed before the repair and pass afterward; a production-host probe on actual Linux confirms the refusal and successful application enable. |
| **Real phone voice acceptance needs attached devices** | `docs/voice-web-mobile.md:44` distinguishes synthetic media/UI checks from an authenticated microphone → computer transcription → agent → speech → phone conversation. The current read-only hardware probe found no Android or iPhone attached. | Device-dependent acceptance remains blocked on hardware availability. The user was asked about connecting the devices while code fixes continued. No physical-phone pass is claimed. |
| **Voice across different networks remains unqualified** | `docs/voice-web-mobile.md:24` records that different-NAT connectivity has not been established; the transport adds no third-party STUN/TURN service. A working local or synthetic connection does not establish remote reachability. | Measure real phone-to-computer connections across different networks and record timeout/recovery behavior; determine transport changes from those results. |
| **Desktop accessibility is still a bounded prototype** | `docs/accessibility-role-tool-verification.md:95` and the adapter implementation limit control to supported UI Automation operations. Terminals, file managers, credential dialogs, raw path entry, and several broader workflows are excluded. Earlier hosted-agent trials used typed requests; they do not establish a sustained live-microphone control workflow. | Define and verify the remaining intended hands-free workflows with actual applications and integrated speech. Preserve the current consent boundary while expanding supported operations. |
| **Physical hand-control acceptance needs the specified camera** | The read-only camera inventory exposes the built-in USB2.0 HD/IR webcam, with no USB Blink camera detected. `docs/accessibility-role-tool-verification.md:102` records the earlier device-specific deferral. | The latest request resumes issue resolution, but the specified hardware is absent. Its tracking and camera-release acceptance cannot be substituted with a different camera or a synthetic pass. |

Linux speech installation is present on this machine. The production runtime resolver found the installed interpreter in LIVE's owner-data directory. Real RTX 4070 GPU/WebRTC smoke tests passed both with the isolated speech environment and with LIVE's installed interpreter/models/cache path, using the current source worker and generated audio. Both produced synthesized audio, transcribed “The blue lantern is on the desk”, exercised interruption, closed the session and exited the worker cleanly (13.00 and 11.55 seconds). This also verifies the previously reported long-path speech setup problem is not reproducing in the installed environment. Local speech still intentionally requires a supported GPU and has no CPU fallback.

Validation for the UI change:

- Focused accessibility and Settings batch after the repair: **90 passed, 12 skipped, zero failures**. The skipped checks require native Windows and, for one test, the explicit Windows GPU speech proof. The real Electron application-control fixture passed on a private Xvfb display, including platform-aware choices, consent, exact click/type/select, stale-target refusal, cross-screen confirmation, panel remounting and Stop.
- Voice and accessible naming/focus batch: **59 passed, zero skips or failures**, including the real Electron voice panel with synthetic media.
- Python speech-runtime contracts: **33 passed**, plus the two real GPU/WebRTC runs described above. Total automated contracts: **182 passed**, 12 platform/hardware skips, zero failures.
- Built renderer: Chromium with Linux and Windows host-capability fixtures, plus compact WebKit with Linux capabilities, passed nine top-level routes each, Settings search and Simple mode, enable/confirm/stop, keyboard rejection, scope restoration, and keyboard exclusion of the unsupported desktop choice. No renderer errors or horizontal panel overflow were observed. These use a synthetic host, not a live account or microphone. An initial browser harness used a generic disabled matcher that does not handle option elements; its failure is retained, and the corrected check verifies the native disabled property and actual keyboard behavior.
- Production renderer build, suite discovery, driver discovery, unbound-identifier check, composed-output check and `git diff --check` passed.
- Full-product test preflight returned its explicit “missing inputs” status because this isolated checkout has no generated capability payload or builder-private source/privacy configuration. The full product suite and native release qualification were not run.

Evidence and repeatable diagnostic scripts are retained at `/home/redacted-profile/voice-accessibility-review-20260908/evidence/`. The original defect is in `linux-desktop-probe.json`; the repaired production-host result is in `linux-desktop-probe-after.json`. New results are in `fixed-accessibility-settings-tests.log`, `fixed-voice-tests.log`, `voice-runtime-unit-tests.log`, `fixed-browser-check.log`, `browser-report.json`, `linux-gpu-speech.log`, `linux-installed-gpu-speech.log` and `hardware-availability.json`. The scope probes used a synthetic owner/role and started no external process or live control session. The GPU probes used generated audio, with no microphone or camera capture, hosted model call, cloud key, or agent task.

Older trial failures were not automatically carried forward: later source and verification records contain voice lifecycle, tool-discovery and role-confinement repairs. The items above distinguish a reproduced current defect from documented feature and acceptance work.
