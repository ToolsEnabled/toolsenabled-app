import { canonical, invariant, sha256 } from './prompts.mjs'
import { runtimeFilesFor, safePath, verifyProject, templateCitationFiles, templateIdentity, TEMPLATE } from './study.mjs'
// THIS IMPORT STAYS, DELIBERATELY. It is the last core-to-vertical edge in this
// file and it was RULED not worth clearing. The reason, so the next person does
// not spend the afternoon I nearly did:
//
// PROVENANCE in lean-codegen.mjs is 10 entries, and 8 of them are CORE
// machinery -- the deterministic zip writer, canonical JSON, SHA-256, the
// cluster percentile bootstrap, sample quantiles, Bonferroni levels, mulberry32
// and Fisher-Yates. Only 2 are Lean's, and those pin the LEAN engine image
// digest, which is why lifting the whole block to core fails on
// LEAN_IMAGE_DIGEST. Clearing this edge therefore means SPLITTING that array
// between core and the plugin, not moving it.
//
// Splitting it reorders ATTRIBUTIONS.md and provenance.json, which are
// customer-facing documents in every exported project, and moves exported bytes.
// Real cost, visible artifact change, no user benefit: the goal was never a zero
// import count. A third party can already register, freeze, export and run.
//
// AND THERE IS A CYCLE WAITING IF YOU DO IT. lean-plugin.mjs would need the two
// Lean entries, which live beside LEAN_IMAGE_DIGEST in lean-codegen.mjs, and
// study.mjs already imports plugins.mjs for registration -- so
// study -> plugins -> lean-plugin -> lean-codegen -> study closes at module-init
// time, exactly when registerBenchmark reads its descriptor. Declare provenance
// as a THUNK rather than an array and nothing is read during init, and the cycle
// never materializes.
import { attributionsMarkdown, provenanceDocument } from './lean-codegen.mjs'
import { auditProjectFiles } from './audit.mjs'
import { observationProjectFiles } from './observations.mjs'
import { compositionProjectFiles } from './composition.mjs'
import { benchmarkFor } from './registry.mjs'
import { requirementProjectFiles } from './requirements.mjs'
import { workflowProjectFiles } from './workflow.mjs'
import { corpusProjectFiles } from './corpus.mjs'
import { analysisProjectFiles, designProjectFiles } from './analysis.mjs'
import { templateProjectFiles } from './templates.mjs'
import { readinessProjectFiles } from './readiness.mjs'

export async function projectFiles(project, sources, attachments = {}) {
  await verifyProject(project)
  const RUNTIME_FILES = runtimeFilesFor(project)
  for (const file of RUNTIME_FILES) invariant(typeof sources[file] === 'string' && project.spec.runtimeSources?.[file] === await sha256(sources[file]), `Freeze the current runtime sources before exporting (${file}).`)
  // Whatever precondition the benchmark that owns this study puts on exporting
  // it. The core does not know what a Lean apparatus review is.
  benchmarkFor(project.spec)?.assertExportable?.(project, RUNTIME_FILES)
  const identity = await templateIdentity(project)
  const generated = 'Generated by ' + TEMPLATE.generator.name + ' ' + TEMPLATE.generator.version + ' with the ' + TEMPLATE.title + ' ' + TEMPLATE.version + (identity ? ' (template runtime identity sha256:' + identity + ')' : '') + '.'
  const studyVersion = project.spec.version
  const packageVersion = studyVersion || '1.0.0'
  const files = { ...sources, ...requirementProjectFiles(project), ...(benchmarkFor(project.spec)?.projectFiles?.(project, sources) || {}), 'project.json': JSON.stringify(project, null, 2) + '\n',
    'specification.json': JSON.stringify(project.spec, null, 2) + '\n',
    'schedule.json': JSON.stringify(project.schedule, null, 2) + '\n',
    'package.json': JSON.stringify({ name: project.spec.id, private: true, version: packageVersion, type: 'module', engines: { node: '>=22' }, scripts: { verify: 'node cli.mjs verify', start: 'node cli.mjs run', analyze: 'node cli.mjs analyze' },
      toolsenabled: { generator: TEMPLATE.generator.name + ' ' + TEMPLATE.generator.version, template: TEMPLATE.id + ' ' + TEMPLATE.version, templateRuntimeIdentity: identity ? 'sha256:' + identity : null } }, null, 2) + '\n',
    'package-lock.json': JSON.stringify({ name: project.spec.id, version: packageVersion, lockfileVersion: 3, requires: true, packages: { '': { name: project.spec.id, version: packageVersion, engines: { node: '>=22' } } } }, null, 2) + '\n',
    'README.md': `# ${project.spec.name}\n\n${studyVersion ? `Study version: ${studyVersion}\n\n` : ''}Frozen project: ${project.sha256}\n\n${generated}\n\nRequires Node 22 or later. This runner has no npm dependencies and works without ToolsEnabled.\n\n1. Extract all files to a new directory.\n2. Acquire any external inputs listed in specification.json, placing them at their declared relative paths. Their exact SHA-256 digests are checked before execution.\n3. Inspect specification.json, prompts/, checklists/, schedule.json, adapters and scoring before running. Set credential environment variables locally; never put their values in the project.\n4. Run \`node cli.mjs verify\`.\n5. Run \`node cli.mjs run\`. Run again to resume; completed trials are never dispatched twice. Use a new --output directory for an independent rerun.\n6. Run \`node cli.mjs analyze\` to derive results.csv and summary.json from attempts.jsonl. It also writes report.html and report.md, which show the composition layers and complete compiled prompt for each exemplar task, the request envelope sent for each condition, a run walkthrough, and every trial's verbatim response and grade. Complete untruncated copies sit under paper/ and trials/; execution-manifest.json lists them all. The same project and journal always produce the same bytes, so this matches the Research page's exported report exactly.\n\nCtrl+C cancels the active attempt. After an actual crash, inspect the journal and process first, then use \`node cli.mjs run --recover\`. Recovery never treats a missing response as success. The fixed attempt budget includes failures and interrupted attempts. ${project.experimentTemplate ? 'Admitted responses and observed resource evidence remain in the journal. Oversized responses retain explicit rejection metadata and a diagnostic prefix.' : 'Every adapter response, failed command stdout/stderr, and grade stays in the journal.'} Time budgets cover active execution, not offline storage time.\n\nThe Research page can run recorded responses with this same runner, import results/evidence.json, or submit this exact cli.mjs to its existing process run board. No separate GUI scoring implementation exists.\n\n## Adapter contract\n\n${project.experimentTemplate ? 'This resource profile supports recorded replay responses and an HTTPS POST endpoint receiving the frozen public prompt, input, requested model settings, trial identity and attempt number. The response envelope supplies output containing one JSON action plan. HTTPS collection requires explicit controls with no tools and matching identity/complete-generation accounting. These reported fields do not authenticate a provider. Custom modules, local command adapters and custom graders are refused for this profile.' : 'A command reads one JSON request from stdin and writes one JSON response to stdout: {\"output\": \"answer\", \"usage\": {}}. No shell is used. The request includes the frozen prompt, input, model settings, trial identity and attempt number. An HTTPS adapter accepts the same contract as a POST. A custom .mjs adapter exports async run(request, {signal}); a grader exports grade(project, task, output) returning {passed, score}. Pin custom modules as attached files before exporting. Command programs must wait for their descendants, honor termination, and report errors using a nonzero exit code. Use a containerized command when provider code needs isolation.'}\n\n## Research decisions\n\n${project.spec.decisions || 'No study rationale was supplied.'}\n\nThis project freezes apparatus and investigator decisions. It does not certify scientific validity, external model determinism, or an approved Lean Bench study. Review records identify the exact bundle content acknowledged by a person; they are not cryptographic identity attestations.\n` }
  if (project.spec.protocol.grading.kind === 'lean-python') files['README.md'] += '\n## Check retained native artifacts\n\nFor a modern schema-2 project, after the run has settled, run `node cli.mjs verify-native`. This command reads the retained journal, all pinned runtime and input bytes, and the candidate/configuration/process/result artifacts. It executes no candidate, grader or provider. On success it writes new `results/native-evidence.json` and `results/native-verification.json`; existing files are never overwritten. Optional `--output DIRECTORY` selects the retained result directory. Keep original artifacts.\n\nIn Research, freeze this exact project, open Run & export, and choose Verify native evidence to import native-evidence.json. The page rechecks the bundle and recomputes the summary before showing a separate verification receipt. Export that receipt before leaving the project; saved drafts do not retain verification. Ordinary run-evidence imports do not verify native artifact bytes.\n\nThis establishes retained-artifact consistency, not authenticated native execution, image/mounted-data provenance, preparation, qualification or experimental admission. Signals, timeouts, unknown completion and invalid engine results are outside this stricter contract; use their existing development/audit inspection paths without relabeling them verified. The command refuses symlinks, incomplete journal writes, files over 16 MiB and total inputs or encoded bundles over 64 MiB. It does not recover a run, change its denominator, or release its native admission hold. Historical audit archives retain their original versioned behavior.\n'
  for (const task of project.tasks) {
    if (task.informationPacket) files['information/' + task.id + '.json'] = JSON.stringify(task.informationPacket, null, 2) + '\n'
  }
  if (project.tasks.some(task => task.informationPacket)) files['README.md'] += '\n## Information review scope\n\nReading consistency concerns the compiled task text. Workflow stages may omit it, add instructions or project prior outputs. One declared task-level reading set applies across conditions; the compiler does not infer readings from those instructions. Version-2 information packets bind condition settings, workflow assignments and the complete declared plan. Inspect information/ and task-reviews.json alongside workflows/ and the actual retained stage requests. Declarations do not establish provider adherence. Legacy version-1 packets retain their task bindings and are not collection-context approval. Existing frozen schema-2 artifacts with packet version 1 require their archived pinned runtime for original verification. Using the current runtime requires a new draft, current packet review and a newly frozen project; the original artifact and review records remain unchanged.\n'
  Object.assign(files, compositionProjectFiles(project))
  files['reviews.json'] = JSON.stringify(project.spec.reviews || [], null, 2) + '\n'
  files['task-reviews.json'] = JSON.stringify(project.spec.taskReviews || [], null, 2) + '\n'
  Object.assign(files, corpusProjectFiles(project))
  Object.assign(files, analysisProjectFiles(project))
  Object.assign(files, designProjectFiles(project))
  Object.assign(files, templateProjectFiles(project))
  Object.assign(files, readinessProjectFiles(project))
  // Where the generated code came from. Written into every export so a
  // reviewer never has to take an attribution on trust from the report.
  files['ATTRIBUTIONS.md'] = attributionsMarkdown()
  files['provenance.json'] = JSON.stringify(provenanceDocument(), null, 2) + '\n'
  // How to cite: the frozen study by its digest and the template by its version
  // and runtime identity. Derived from the project alone, so the report's
  // paper/CITATION.* copies are the same bytes.
  Object.assign(files, await templateCitationFiles(project))
  // A26: the export carries its own licence. The text is a pinned runtime constant rather
  // than a file read, because an export is generated in the browser, and the scope line
  // states what the licence covers so a recipient need not infer it.
  files['LICENSE'] = TEMPLATE.licenseText + '\n'
    + 'This ' + TEMPLATE.license + ' licence covers ' + TEMPLATE.licenseCovers + ', including this\n'
    + 'generated project and everything under it that the template produced. Task inputs, model\n'
    + 'outputs and any attached files you supplied are yours and are not licensed by this file.\n'
  if (project.nativePreparation) {
    files['native-preparation/plan.json'] = JSON.stringify(project.nativePreparation, null, 2) + '\n'
    files['native-preparation/README.md'] = '# Generated native preparation plan\n\n' + project.nativePreparation.scope
      + '\n\nThis manifest binds the registered selected task/readings, occurrence mutations and probes. Inspect every coverage row, gap and separate apparatus requirement. Job and repetition counts are planned work, not executed controls. Native admission remains unavailable.\n\nIn Research, freeze this exact draft, choose one frozen control job under Run & export, and export its separate control ZIP. The child project contains generated reference or mutant code and a pinned native-control/binding.json linking its origin to this parent. It retains apparatus-development purpose and a singleton expected observation for that program. No child project is embedded in this parent export.\n\nA successful control must agree with its own exact native observation. Mutants also require a separate later comparison with their baseline; a failed run or a cross-contract refusal is not successful discrimination. Preserve source, image, mounted data, raw native artifacts and process/ownership evidence. The strict verify-native command checks retained artifact consistency only.\n\nThe plan does not implement an owned preparation runner or aggregate preparation receipt. Running separate child ZIPs does not enforce the combined execution or duration budget across jobs or invocations. Host/data provenance, complete native control coverage, preparation accounting, personal reviews and experimental admission remain separate requirements. No execution or approval is inferred from this export.\n'
    files['README.md'] += '\n## Native preparation plan\n\nInspect native-preparation/plan.json and its README for the complete generated roster, gaps, planned repetitions and declared budgets. Export one selected control project from Research after freezing this parent. These are generated apparatus controls awaiting execution and verification; individual ZIPs do not enforce the aggregate preparation budget or establish native experimental admission.\n'
  }
  files['README.md'] += '\n## Licence\n\nLICENSE beside this file is the ' + TEMPLATE.license + ' licence, covering ' + TEMPLATE.licenseCovers + '. Your task inputs, the model outputs and any files you attached are yours and are not licensed by it.\n'
  files['README.md'] += '\n## Attribution and reused code\n\nATTRIBUTIONS.md and provenance.json record, for every artifact this project generates, which parts are reused from another project, which are adapted and which are newly generated here. Reused or adapted material carries its upstream project, authors, repository, licence notice, exact revision and the SHA-256 of the upstream bytes; material generated here carries no upstream hash, because a hash there would assert a reuse that did not happen. Specifications and papers our own implementations follow are listed separately and marked as contributing no code.\n'
  files['README.md'] += '\n## Cite this study and its template\n\nCITATION.cff and CITATION.bib name this frozen study by its project digest and the template that generated it, ' + TEMPLATE.title + ' ' + TEMPLATE.version + ', with the template runtime identity (a SHA-256 over the pinned runtime sources) and the primary references of the statistical methods the frozen analysis plan uses. The report written by `node cli.mjs analyze` repeats both under paper/ and lists every reference it cites.\n'
  files['README.md'] += '\n## Execution eligibility\n\nThis export separates source integrity, recorded diagnostics, apparatus development, required preparation and experimental collection. Inspect readiness/contract.json for a version 2 experiment and run `node cli.mjs readiness` for the same derived decision used by Research and the runner. A source-valid project or a declared answer passing its own grading rule does not establish a qualified experiment.\n\nNew experiments declare the supported schedule, repeated-measurement meaning, binary primary endpoint, dependence treatment and exact primary population. The runner refuses unsupported or incomplete capabilities before collection, and obtains the required fresh qualification for each invocation that can collect. A static eligible result means the design can enter its required preflight; it never replaces that execution evidence. Cohort labels and caller-supplied flags cannot grant eligibility.\n\nVersion 1 projects retain their exact original bytes and specific evidence. The current runtime permits their canonical recorded replay diagnostics and completed resume without new collection. New scientific collection requires a version 2 experiment with its supported fields and controls. Recorded-diagnostic mode permits only canonical replay and supported built-in grading. To build and test an unfinished adapter or grader, explicitly select apparatus-development in the qualification cohort with finite budgets. Its computations remain unqualified development evidence in the journal and every analysis report; changing a cohort label or passing a control cannot turn them into scientific results. Development mode retains all applicable selected-input, resource, native, process and personal-review gates. Existing exported runtimes retain their own versioned behavior; this runtime cannot retroactively change their records.\n'
  if (project.experimentTemplate) files['README.md'] += `\n## Generated resource experiment\n\nEdit the resource template fields in Research: cases and their families/splits, initial resource values, public visibility, write capabilities, required goals, action budget and primary criterion. Generate the experiment, review its conditions and analysis plan, then freeze and export. The compiler derives the prompts, public inputs, complete private fixtures, reference plans, control plans and grading contract. Inspect templates/recipe.json, templates/contract.json and templates/cases/. Generated tasks and criteria must match regeneration from the frozen recipe; editing one generated file cannot change the experiment independently. A generated control plan is awaiting execution.\n\nBefore any new trial in each invocation, the runner durably records template-preparation-started with its effective timeout and recovery reservation, then executes fresh fixed controls with hand-computed expectations and controls for the exact selected cases. A separate state verifier checks their observations against the frozen source and fixtures. The first successful qualification is retained in full; later successful qualifications must match that full proof and retain compact hash references. Failed, cancelled or timed-out preparation cannot authorize collection. Its reason and measured time charge remain in a terminal paired to that preparation intent. Explicit recovery of an open preparation records interrupted work with unknown elapsed time and the complete reserved charge before any fresh conformance check. This pure in-memory preparation owns no external child process. Interrupted resource episodes and unresolved selected-input interpreters retain their separate halt rules. The run command enforces this gate automatically.\n\nEach evaluated response supplies one JSON action plan for a fresh bounded in-memory resource map. Trusted code applies set/delete operations to the fixed universe and observes state after every admitted action. Visibility controls the public request; writability controls which effects are permitted; goal resources define the intended task scope. A writable non-goal change is executed and measured as collateral. A rejected action stops the plan before that action takes effect and makes the plan invalid. Resource values, plan bytes and action count have separate frozen limits. This profile measures synthetic map effects; filesystem/service changes, arbitrary programs, interactive tools and shared or persistent execution state are unsupported.\n\nThe CLI retains the raw response before executing its plan, and durably records preparation, action intentions, observed effects and closure in results/attempts.jsonl. Inspect results/responses/ and the generated report as well. Source, response and snapshot hashes bind the retained evidence, and journal verification reconstructs effects independently; these checks establish consistency of retained records. Oversized transport responses retain a bounded diagnostic prefix and explicit incompleteness instead of invented complete bytes. The Research page uses the same portable execution and verification logic with evidence held in memory; export its evidence before closing or changing the page.\n\nEvery scheduled trial has one attempt. Completed trials can be resumed past without recollection. A failed or interrupted trial halts this resource study, including failures before effects begin. Inspect the retained evidence and repair the design in a new frozen project before further collection. After a crash, the CLI refuses recovery when an orphan already has a retained response file. Otherwise --recover can close the orphan as interrupted and reserve its timeout; it cannot execute the missing remainder or redraw the response. After a durable-write failure, inspect the partial journal and any retained lock. Incomplete episodes have no completed endpoint; any observed prefix remains separately labeled.\n\nPreparation charges and recorded attempt durations accumulate across invocations against protocol.maxDurationMs. Each preparation and attempt also uses the remaining active-run deadline; offline time is excluded. At most ${project.schedule.length + 1} preparation receipts are admitted for this ${project.schedule.length}-trial schedule. The compiler refuses designs whose conservative retained-evidence projection exceeds 16 MiB, and the runner separately caps the cumulative canonical journal at ${project.experimentTemplate.limits.evidenceBytes} bytes before each append. Reducing cases, actions, resources, conditions or replicates can make a refused design fit; changing a frozen limit requires a new export.\n\nThe frozen primary criterion is ${project.experimentTemplate.primaryCriterion}. Only that binary criterion supplies primary rates and planned contrasts, using all scheduled trials in the declared primary population. Task success remains separate from ever-changed, final-state and peak collateral measurements. Repairing a resource can clear its final-state difference while retaining its earlier collateral effect. Effect sets and counts are descriptive outcomes. Missing complete observations remain unavailable, and rejected actions and rejected envelopes are counted separately. Fresh resource reset does not establish independent task sampling or model randomness.\n`
  if (project.design) files['README.md'] += '\n## Grouped assignment design\n\nInspect design/plan.json: the frozen arms (condition groups), draw units, member indices, ordered phases and every trial identity a phase left unscheduled. Each schedule row carries its arm, unit and phase. Arms, draws and phases are representation and analysis grouping; they do not change how a trial runs, its budgets or its admission, and a phase decision is recorded only by a separate stop-rule evaluation.\n'
  files['README.md'] += '\n## Primary analysis and custom outcomes\n\nThe frozen analysis plan selects its primary population: all tasks, an explicit development/held-out split, or a named task set. New split/set exports bind analysis/population.json. Primary rates and planned contrasts use that roster; the complete schedule, dispositions and resource accounting remain retained. Unsupported analysis fields refuse instead of becoming ignored declarations. See primary-population.csv and primary-rates.csv in the generated report.\n\nCustom graders return complete deterministic finite JSON with passed and score; process is reserved for the host receipt. Journal/report checks compare every result field against retained process stdout when present. The CLI also reruns the pinned module before accepting stored custom outcomes; fresh logs never replace the original process receipt. Browser consistency checks do not execute Node modules or authenticate an execution. Direct API grades without process receipts retain that evidentiary limitation.\n'
  Object.assign(files, auditProjectFiles(project))
  Object.assign(files, observationProjectFiles(project))
  Object.assign(files, workflowProjectFiles(project))
  if (project.workflows) files['README.md'] += '\n## Frozen prompt workflows\n\nInspect workflows/plan.json and workflows/contract.json. Each assigned condition follows one sequential route through its acyclic graph. Ordered branches compare retained stage output with frozen JSON; the terminal result path is selected before grading. Each stage gets a fresh public request with its exact instructions, declared task visibility and declared ancestor output projections. No implicit conversation history is passed. Adapters receive workflow metadata and the allowed tool definitions in collection.tools. They must return workflow.toolCalls, an explicit array of {id,name,arguments,result,status} records (empty when none are reported). The host checks reported actions and frozen byte/token/tool budgets; opaque external tool access and provider context enforcement remain unobserved. Every stage start is durable before dispatch and every raw stage response enters attempts.jsonl before another stage or the grader runs.\n\nAny workflow error, policy violation or interruption halts collection. Recovery closes an uncertain attempt, reserves its entire timeout and retains all stages; it cannot redraw or continue a partial workflow. Inspect and repair apparatus in a new frozen project before reusing retained outputs. Per-stage identity, usage, cost, requests and tool logs remain in workflows.json and the report tables. Trial accounting includes every started stage, including intermediate answers; unavailable stage usage prevents a full total.\n'
  files['README.md'] += '\n## Judge audit references\n\nFor a source study, run `node cli.mjs reference --output RESULTS_DIRECTORY` to seal its frozen project, complete attempt journal, pinned source/input bytes and native evidence used by completed traces. Import the resulting reference-bundle.json in Research → Benchmark builder → Judge audit. Declare provenance, criterion, case selection, judge configuration and analysis before reviewing and freezing the new audit. A source bundle records reproducible bindings, not authenticated execution or approval.\n\nAn audit export retains its source bundle, selection ledger, case review packets and review records under audit/. Judges return {"verdict":"accept","reason":"Optional explanation"}. Allowed verdicts are accept, reject, undetermined and abstain. Agreement is relative to the declared reference criterion. Unresolved reference cases have null scores and remain outside the frozen reference-eligible rate population. Source observations and later judge verdicts remain separate. `qualify` rechecks audit bindings without running a real judge or claiming a new native source execution. Reports retain all trials, eligible denominators, confusion tables and source identities.\n'
  if (project.spec.observationPlan) files['README.md'] += '\n## Attempt accounting\n\nInspect observations/plan.json and observations/contracts.json before collection. Mapped identity, completion, usage and cost remain adapter reports; requested model/settings/collection controls are separate declarations. Completion accepts complete or incomplete; arbitrary finish reasons are not inferred. Required identity or completion failures and successful-transport extraction failures retain evidence and stop collection without redrawing a response. A replay mode of "envelope" retains recorded metadata, not new provider usage.\n\nEvery started attempt enters observations.json and the complete report tables, including retries, failed attempts, cancellations and interrupted/open records. Known subtotals include only observed values; a full total remains null when any attempt is missing. Explicit zero differs from unavailable. Reported charges and declared estimates stay separate by currency and metadata origin. Estimates need pinned price/allocation sources. Grader/infrastructure charges and a complete experiment cost remain unobserved.\n\nHost timing uses nested monotonic microsecond intervals from before the durable start through settlement. Terminal journal writing, between-attempt work, preparation and later verification are outside the interval. Inclusive phases contain their children; exclusive durations partition measured attempt time. Native execution includes container/engine startup. Provider generationMs is separate. Recovery reserves timeout budget without inventing elapsed time.\n'
  files['README.md'] += '\n## Qualification and LEAN code grading\n\nRun `node cli.mjs qualify` to retain local apparatus checks in results/qualification.json. `node cli.mjs analyze` reads that receipt and the native verification receipt (results/native-verification.json, written by `node cli.mjs verify-native`) beside the journal, prints each under its SHA-256 in the report (the pre-collection qualification receipt in the method section, the native verification receipt in the integrity section), retains them under paper/, and embeds them with their digests in results/evidence.json, so the Research page renders the same receipts when that evidence file is imported. Without them the report says Not declared; a receipt for a different project or journal stops analyze. For Lean Bench this includes hand-authored mixed-tree controls and independent JavaScript/Python interpretation of every frozen task. Python 3.10 or later is required; `--python COMMAND` selects it. These checks do not run an LLM or replace native engine execution.\n\nFor LEAN code grading, select `protocol.grading.kind: "lean-python"` and pin `environment.leanImage` to an immutable Docker digest before freezing. Pull that exact digest locally. The runner uses Python to generate deterministic equity minute files from task inputs and reads market metadata from the pinned image. Each returned FrozenBenchmark Python program runs in an owned, disposable Docker container with networking disabled and resource limits. The frozen executionTimeoutMs is separate from the whole attempt timeout. Native order artifacts are graded; printed trace claims are ignored. Source, logs, orders, traces and ownership records remain under results/artifacts. No repository or ToolsEnabled services are required.\n'
  if (project.requirements?.selectedInput) files['README.md'] += '\n## Selected-input qualification gate\n\nThe frozen requirement policy is ' + project.requirements.selectedInput.policy + '. The qualifier tests the exact selected input for each registered task/reading/requirement as well as its separate hand-checked probes. Every registered wrong reading must be observably distinguished while the selected requirement activates. Runtime appendices remain separately unassessed. A require-composition policy also refuses any unregistered composition occurrence or admissible reading.\n\nFor either required policy, `node cli.mjs run` obtains fresh local independent qualification before new collection in each invocation. Before entering the qualifier, attempts.jsonl durably records qualification-started with its effective timeout, a 5000 ms cleanup allowance and their combined recovery reservation. A successful qualification or qualification-failed terminal binds preparationSeq to that start. Successful, failed, cancelled and timed-out preparation charges include measured work through qualifier settlement and accumulate with attempt charges across resumes. A valid proof that exhausts the remaining study budget admits no trial. These measurements exclude offline gaps and do not claim total study wall-clock time. Completed trials are never dispatched again.\n\nThe CLI binds raw preflight evidence under results/preflight/<preparationSeq>-<id>/: started.json and failure.json identify the preparation; qualification.json remains the complete original proof. A source-valid proof file alone cannot replace the paired journal gate. If cancellation leaves the qualifier unresolved after its five-second cleanup allowance, the intent and run lock remain. Explicit `run --recover` can record interrupted accounting with unknown elapsed time and the full reserved charge; that journal stays halted because interpreter termination is unconfirmed. Inspect and stop owned work before repairing the apparatus. A new project identity does not establish termination.\n\nThe current runtime permits at most maxTotalAttempts + 1 preparation starts, a 4 MiB canonical full qualification event, a 16 MiB cumulative canonical modern journal and 32 MiB of serialized event rows. These UTF-8 limits do not bound the total output directory, report files or the complete evidence.json including its summary. An oversized accepted proof becomes a bounded failed-preparation diagnostic; its raw CLI preflight file remains, and no candidate is dispatched. Research separately limits evidence file imports to 32 MiB.\n\nLegacy proofs without paired timing remain inspectable with unavailable preparation duration and charge. Missing time is never zero. Completed legacy studies remain compatible with read-only analysis and completed resume; unfinished legacy journals cannot begin new preparation when cumulative charges are unknown. The report retains proof and preparation availability separately.\n\n`node cli.mjs qualify` is an explicit diagnostic apparatus check outside this run-preparation ledger. Its qualification.json cannot substitute for fresh qualification during run or reset a run budget. All qualification evidence remains internally checked and source-bound; it does not authenticate execution, establish native validation, approve private or personal decisions, or authorize a counted study.\n'
  if (project.requirements?.interpreters) files['README.md'] += '\n## Generic qualification interpreters\n\nThe requirementPlan.interpreters fields reference and independent name distinct .mjs attachments pinned in spec.inputs; rationale explains their independent designs. Both export interpret(task, target, options) and return { observation, activation: { counters, transitions } }. task supplies id, root, variables, input and compiled semantic/composition data. target is null for a whole-task check or { requirement } for an occurrence. Expected answers, probe assertions, model responses and wrong-reading identities are withheld. The host compares complete counterpart results, then checks the declared assertions and activation rules.\n\nEach call runs in an owned Node child process with a 60-second ceiling, the overall qualification deadline and bounded output. The parent kills the process group on cancellation and retains stdout, stderr, exit status, exact request and source bindings. These are local executable modules with normal filesystem access, like custom adapters; they are not a sandbox. Declare and pin their supporting files and dependencies. Distinct source hashes and agreement are auditable evidence, not proof of scientific independence or universal correctness. LEAN keeps its built-in JavaScript/Python counterpart.\n'
  for (const [file, contents] of Object.entries(attachments)) {
    invariant(safePath(file) && !Object.hasOwn(files, file) && file !== 'manifest.json' && !file.startsWith('results/'), `Attachment would overwrite a project file: ${file}.`)
    invariant(typeof contents === 'string', `Attachment ${file} must be text.`)
    invariant(project.spec.inputs.some(input => input.path === file && input.sha256), `Pin attached file ${file} in the input manifest before freezing.`)
    files[file] = contents
  }
  for (const input of project.spec.inputs) {
    if (Object.hasOwn(files, input.path)) invariant(await sha256(files[input.path]) === input.sha256, `Attached input hash differs: ${input.path}.`)
  }
  for (const condition of project.spec.conditions) if (condition.adapter.kind === 'module') invariant(Object.hasOwn(files, condition.adapter.file), `Attach ${condition.adapter.file} before exporting.`)
  if (project.spec.protocol.grading.kind === 'module') invariant(Object.hasOwn(files, project.spec.protocol.grading.file), 'Attach the custom grader before exporting.')
  if (project.requirements?.interpreters) for (const kind of ['reference', 'independent']) invariant(Object.hasOwn(files, project.requirements.interpreters[kind].file), 'Attach both qualification interpreter modules before exporting.')
  invariant(Object.keys(files).every(safePath), 'The export contains an invalid project path.')
  invariant(new Set(Object.keys(files).map(file => file.toLowerCase())).size === Object.keys(files).length, 'Project file names must be distinct on case-insensitive filesystems.')
  const digests = Object.fromEntries(await Promise.all(Object.keys(files).sort().map(async file => [file, await sha256(files[file])])))
  files['manifest.json'] = canonical({ format: 'research-benchmark-files', version: 1, projectSha256: project.sha256, files: digests }) + '\n'
  return files
}

export function crc32(bytes) {
  let crc = 0xffffffff
  for (const byte of bytes) { crc ^= byte; for (let i = 0; i < 8; i++) crc = crc & 1 ? (crc >>> 1) ^ 0xedb88320 : crc >>> 1 }
  return (crc ^ 0xffffffff) >>> 0
}
// Deterministic, UTF-8, uncompressed ZIP; no platform tools or dependencies.
export function zipFiles(files) {
  const encoder = new TextEncoder(), local = [], central = []
  let offset = 0, centralSize = 0
  for (const name of Object.keys(files).sort()) {
    invariant(safePath(name), `Invalid archive path: ${name}.`)
    const file = encoder.encode(name), bytes = encoder.encode(files[name]), crc = crc32(bytes)
    const header = new Uint8Array(30 + file.length), view = new DataView(header.buffer)
    view.setUint32(0, 0x04034b50, true); view.setUint16(4, 20, true); view.setUint16(6, 0x0800, true)
    view.setUint16(12, 33, true); view.setUint32(14, crc, true); view.setUint32(18, bytes.length, true); view.setUint32(22, bytes.length, true); view.setUint16(26, file.length, true); header.set(file, 30)
    const entry = new Uint8Array(46 + file.length), e = new DataView(entry.buffer)
    e.setUint32(0, 0x02014b50, true); e.setUint16(4, 20, true); e.setUint16(6, 20, true); e.setUint16(8, 0x0800, true); e.setUint16(14, 33, true)
    e.setUint32(16, crc, true); e.setUint32(20, bytes.length, true); e.setUint32(24, bytes.length, true); e.setUint16(28, file.length, true); e.setUint32(42, offset, true); entry.set(file, 46)
    local.push(header, bytes); central.push(entry); offset += header.length + bytes.length; centralSize += entry.length
  }
  const end = new Uint8Array(22), e = new DataView(end.buffer)
  e.setUint32(0, 0x06054b50, true); e.setUint16(8, central.length, true); e.setUint16(10, central.length, true); e.setUint32(12, centralSize, true); e.setUint32(16, offset, true)
  const result = new Uint8Array(offset + centralSize + end.length)
  let index = 0
  for (const part of [...local, ...central, end]) { result.set(part, index); index += part.length }
  return result
}
