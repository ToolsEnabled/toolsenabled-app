# Controller disposable native recipe

Preparation only. App 07091006f633d8e452ea9b6a27d0281c88f6249b; engine f3d8bb3d81fb6a1868a13b2c4f1dbb1dafd0544c. No acceptance tree was identified at handoff. Controller must identify an authorized disposable native tree/profile and attest its loaded pair before execution. Do not use the owner LIVE profile, restart/promote an app, run a paid provider, or change QA fixtures. If an authorized non-paid native provider is unavailable, leave the receipt incomplete. This driver reads evidence; it never launches a native app or provider.

1. In the isolated app worktree run `node tools/paste-resume-native-acceptance.mjs template > /path/to/disposable-evidence/receipt.json` (create the evidence directory first). Record Controller run identity, disposable profile and mode. Use `native` only for actual native observations; `mock` remains nonqualifying.
2. Prepare a disposable image containing a fresh visual challenge (random shapes/colors or characters). Do not disclose the expected challenge in prompt text. Record expected challenge privately in evidence, along with image and native interaction recording. On agent page and session surface, paste using the native clipboard in both first-send and continuation flows. Add ordinary words; send exactly once. Capture the resulting message, one attachment, preserved words, and provider response correctly reading the image-only challenge. A preview, IPC send success, synthetic base64, or fake provider acknowledgment is insufficient. Record provider receipt/message ID and transcript evidence. Set fields named in the validator only from observations.
3. Separately exercise native capture through the desktop worker to an MCP image content block and actual provider observation of a fresh challenge. Record the capture, MCP image block metadata, response and receipt. Composer paste does not prove this J7 boundary.
4. In disposable saved conversations, test Codex saved/current Claude and Claude saved/current Codex via native tree Resume. Capture before/after session IDs, transcript, visible refusal naming both providers, and event/process traces covering the whole action. Require zero close/start/replacement calls and preserved existing session/transcript. Absence of a UI error alone is insufficient.
5. Test Codex/Codex, Claude/Claude and a disposable legacy transcript without provider metadata. Capture saved and resumed thread IDs, normal resume and no refusal. Legacy data must be created in the disposable profile only. No shared fixture edits.
6. Put nonempty evidence files beside receipt.json and reference relative paths in every case. Populate observed keys from `tools/paste-resume-native-acceptance.mjs`: paste/capture use nativeImage, providerReceivedImage, visualChallenge, providerAnswer, receiptId; paste also wordsPreserved/attachmentCount; capture also mcpImageBlock. Mismatch uses refusal, visible, closeCalls/startCalls/replacementCalls, sessionBefore/sessionAfter, transcriptPreserved. Controls use resumed, refused, savedThread/resumedThread.
7. Run `node tools/paste-resume-native-acceptance.mjs validate /path/to/disposable-evidence/receipt.json`. Exit 1 means incomplete; exit 2 is input/usage failure. Exit 0 means structured evidence is ready for independent review, never acceptance. The validator cannot authenticate observations or prove that a referenced log supports a claim; Manager/Controller must inspect originals and provenance. Retain failures and blocked cases; never fill missing observations with mock values.

Offline tests exercise the evidence contract only. They neither rerun completed product suites nor prove native/provider behavior. Return receipt, original evidence, exact loaded pair and command output through Controller to Manager 2.

## Required provenance packet for Controller

Use a new evidence directory under the exclusive `paste-resume-native-acceptance` prefix; preserve the existing unexecuted receipt and failed observations. Before the run, record Controller identity, UTC start/end, authorized disposable tree/profile, operating system, app process identity and loaded executable location, and runtime app/engine revision evidence. A checkout HEAD alone does not prove the loaded runtime pair. Keep preparation/driver revision separate from runtime revisions. Record provider identity and authorization for the non-paid run. If any prerequisite is unavailable, report the missing prerequisite and leave affected cases incomplete.

Create `provenance.md` beside the new receipt. Index each case ID to original recording, transcript, trace and image files, their SHA-256 digests, capture timestamps, and relevant event ranges. Reference these contained relative files in the case's evidence array. Explain how clocks and action IDs correlate across renderer, bridge, host and provider records; retain originals, failures and any gaps. Exclude credentials. Hashes protect packet consistency but do not authenticate the claimed observations. Controller must inspect the originals and record a per-case supported/unsupported finding with reasons in `provenance.md`; validator exit 0 cannot replace this review.

## Directional Resume observations

| Case | Saved metadata | Current provider | Required visible refusal meaning |
| --- | --- | --- | --- |
| resume-codex-claude | Codex | Claude | The saved Codex conversation cannot continue with current Claude. |
| resume-claude-codex | Claude | Codex | The saved Claude conversation cannot continue with current Codex. |

Record the literal rendered wording and a screenshot showing it, plus saved provider metadata and current provider selection before the action. Merely containing both names is insufficient: verify each name has the correct role. Record node ID, existing session ID, saved thread ID, and transcript before and after one native tree Resume action. Preserve original transcript content as well as its identity.

Start action tracing before the click and retain it through refusal rendering and completion of pending work. Identify the trace sources and instrumentation coverage for session close, agent start and session replacement, including relevant bridge/host events and process lifecycle observations. Correlate the single action across all sources. A quiet process list or missing event is not proof of zero calls: explain why each operation would have appeared, record dropped-event/coverage gaps, and leave the case incomplete if coverage cannot establish zero close/start/replacement calls. Do not add shared instrumentation without collision review.

For Codex/Codex and Claude/Claude controls, retain saved/current provider metadata, original saved thread identity, native Resume action and actual resumed thread evidence. For legacy, retain disposable saved metadata demonstrating that the provider field is absent. Prove original-thread continuation and absence of refusal for all three; a newly created thread does not qualify.

## Image-only receipt observations

For each of the four paste cases, record actual surface identity, first-send versus continuation state, session/thread IDs before and after, one native paste and one send. First-send must establish the resulting session; continuation must retain its existing session/thread. Record the full outgoing ordinary text, exactly one image attachment, native clipboard interaction and provider response/message identity. Inspect the transcript to confirm the challenge answer was never supplied in prompt text, prior conversation, attachment filename or other provider-visible metadata. Use a fresh image-only challenge for each case and retain expected answer separately from provider input. Record the original provider answer; do not rewrite it to satisfy equality. If the validator's exact-answer comparison rejects an otherwise interpretable response, retain the rejection for manual review.

For capture-mcp, retain the actual desktop source and capture action, capture artifact, reconstructed MCP image content with MIME metadata, and evidence connecting that content to the provider request and response IDs. Show the provider correctly observed a fresh image-only challenge. A paste receipt or MCP success flag cannot substitute for this separate path. For every image case, correlate the actual provider receipt to the image-bearing request and response; a UI preview, send acknowledgment, copied expected answer or unrelated provider message cannot qualify.

## Preserved dissent and limits

Dissent/limits: do not close all R51 on this renderer correction. Historical successor 427cab93 adds agent:start allow-list coverage; not imported or executed in this lane. Engine wording says resumed while renderer/shell say continued, documented historical discrepancy; no copy redesign. Do not claim R49 or J7 closed from ancestry or mocked bridge checks.

Validator checks claims and referenced files, cannot authenticate native provenance/content; independent review required, accepted always false.
